def generate_answer(prompt: str, user_input: str) -> str:
    return (
        "LLM response placeholder.\n"
        "The real model will be connected in the next step.\n\n"
        f"User input: {user_input}"
    )